name: golangci-lint
on: 
  push:
    tags: 
      - v*
    branches:
      - master
      - main
  pull_request: 

permissions:
  contents: read
  pull-requests: read

jobs:
  lint:
    uses: jimschubert/.workflows/.github/workflows/go-lint.yml@main
    with:
      go-version-file: go.mod
      golangci-lint-version:  v2.7.2
